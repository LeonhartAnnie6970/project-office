-- -- Add image columns to tickets table
-- USE helpdesk_nlp;

-- ALTER TABLE tickets 
-- ADD COLUMN image_user_url VARCHAR(500) DEFAULT NULL AFTER description,
-- ADD COLUMN image_user_filename VARCHAR(255) DEFAULT NULL AFTER image_user_url,
-- ADD COLUMN image_admin_url VARCHAR(500) DEFAULT NULL AFTER image_user_filename,
-- ADD COLUMN image_admin_filename VARCHAR(255) DEFAULT NULL AFTER image_admin_url;

-- -- Verify changes
-- DESCRIBE tickets;

-- -- Test query
-- SELECT id, title, image_user_url, image_admin_url FROM tickets LIMIT 5;